#ifndef YDSH_CONFIG_H
#define YDSH_CONFIG_H

// clang-format off

// some system information

#define X_INFO_SYSTEM  "${CMAKE_SYSTEM}"

#define X_INFO_CPP     "${CMAKE_CXX_COMPILER_ID}"

#define X_INFO_CPP_V   "${CMAKE_CXX_COMPILER_VERSION}"

#define X_INFO_VERSION "${YDSH_VERSION}"

#define X_INFO_VERSION_CORE "${PROJECT_VERSION}"

#define X_INFO_MAJOR_VERSION   ${PROJECT_VERSION_MAJOR}

#define X_INFO_MINOR_VERSION   ${PROJECT_VERSION_MINOR}

#define X_INFO_PATCH_VERSION   ${PROJECT_VERSION_PATCH}

#define X_CONFIG_DIR "${CMAKE_INSTALL_FULL_SYSCONFDIR}/ydsh"

#define X_DATA_DIR "${CMAKE_INSTALL_FULL_DATADIR}/ydsh"

#define X_MODULE_DIR "${CMAKE_INSTALL_FULL_DATADIR}/ydsh/module"


// extra option

#cmakedefine USE_LOGGING

#cmakedefine USE_SAFE_CAST

#cmakedefine FUZZING_BUILD_MODE

#cmakedefine USE_PCRE

#endif //YDSH_CONFIG_H