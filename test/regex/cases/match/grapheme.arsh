
source $SCRIPT_DIR/../syntax/test_helper.arsh

# \X
assert diff <(redump -m '🇯🇵🇯🇵🇯🇵' '\X' 'u') <(<<'EOF'
input: `🇯🇵🇯🇵🇯🇵'
(offset=0, size=8)
EOF
)

# \X{3}
assert diff <(redump -m '🇯🇵🇯🇵🇯🇵' '\X{3}' 'iu') <(<<'EOF'
input: `🇯🇵🇯🇵🇯🇵'
(offset=0, size=24)
EOF
)

assert diff <(redump -m '🇯🇵🇯あ2' '\X{3}' 'u') <(<<'EOF'
input: `🇯🇵🇯あ2'
(offset=0, size=15)
EOF
)