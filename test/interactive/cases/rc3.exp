#!/usr/bin/env expect

# RC file loading failed

set bin_name [lindex $argv 0]
spawn ${bin_name} --rcfile hogehfuaeriv

set CTRLD \004
set timeout 1

expect {
    -re "\\$ $" {
        send "12\r"
    }
    default {
        send_error "mismatch first prompt"
        exit 1    
    }
}

expect {
    "(Int32) 12" {
    }
    default {
        send_error "mismatch"
        exit 1
    }
}

expect {
    -re "\\$ $" {
        send $CTRLD
    }
    default {
        send_error "mismatch prompt"
        exit 1
    }
}

expect default
catch wait result
set os_error [ lindex $result 2 ]
if { $os_error == -1 } {
    send_error "execution failed"
    exit 1
}
set status [ lindex $result 3]
exit $status
