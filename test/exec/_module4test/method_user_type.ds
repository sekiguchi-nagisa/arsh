
typedef Interval(a : Int, b : Int) {
  let begin = $a
  let end = $b
}

function dist() : Int for Interval {
  return $this.end - $this.begin
}


typedef APIError : Error

function getKind() : String for APIError {
  return $this.message().split(": ")[0]
}