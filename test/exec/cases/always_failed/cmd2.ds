# not allow String Array type in redirection target

#$test($result = 'type', $lineNum = 6, $chars = 14, $errorKind = 'Unacceptable', $status = 1)


echo hello > ${["fer", "fr"]} 54fherui   # illegal target