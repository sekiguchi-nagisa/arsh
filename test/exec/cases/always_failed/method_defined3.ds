#$test($result = 'type', $lineNum = 5, $chars = 2, $errorKind = 'SameModOfRecv', $status = 1)

source ../../_module4test/module1.ds

 function comapre(e : APIError) : Bool for APIError {
    return $this.message() < $e.message()
}