#$test($result = 'type', $status = 1, $errorKind = 'Unreachable', $lineNum = 9)

for(var i = 0; $i < 10; $i++) {
    if($i < 0) {
        break
    } else {
        continue
    }
    assert($false)
}