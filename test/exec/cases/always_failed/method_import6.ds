#$test($result = 'type', $lineNum = 7, $chars = 10, $errorKind = 'DefinedMethod', $status = 1)

source ../../_module4test/method.ds as mod

function factorial() for Int {}

function factorial = mod.factorial for Int
