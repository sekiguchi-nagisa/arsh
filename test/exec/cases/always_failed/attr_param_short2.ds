#$test($result = 'type', $lineNum = 9, $chars = 21, $errorKind = 'InvalidShortOpt', $status = 1)

var ss = "s"

[<
  CLI >]
typedef AAA() {
    [<Flag(short: 'b')
    >] [<Flag(short:'aa')>]
  var a = $false
}
