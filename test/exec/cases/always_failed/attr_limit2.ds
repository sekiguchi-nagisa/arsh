#$test($result = 'type', $lineNum = 10, $chars = 5, $errorKind = 'AttrLimit', $status = 1)

[<CLI CLI() CLI() CLI CLI
  CLI CLI CLI>]
typedef AAA() {
  var a = ""

    [<Option Option(short: 'a') Arg() Arg
    Arg Option(long: 'aa') Option(long: $OSTYPE) Option(long: 'sss')
    Arg()>]
    var b = ""

}
