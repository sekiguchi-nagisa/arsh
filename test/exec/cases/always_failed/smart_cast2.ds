#$test($result = 'type', $lineNum = 9, $chars = 13, $errorKind = 'UndefinedBinary', $status = 1)

{
    var aa = "2345".toInt()
    function() => {
        $aa = $none
    }
    if $aa {
        $aa + 34  # $aa is captured, so does not perform smart-cast
    }
}