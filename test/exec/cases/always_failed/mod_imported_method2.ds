#$test($result = 'type', $lineNum = 5, $chars = 14, $errorKind = 'UndefinedMethodInMod', $status = 1)

source ../base/mod_method2.ds as mod

function mod.factorial for Int as fff   # factorial is imported in `mod_method2.ds`, but not import
