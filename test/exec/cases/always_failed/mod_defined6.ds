##$test($result = 'type', $lineNum = 5, $chars =10 , $errorKind = 'DefinedMethod', $status = 1, $fileName = $0)

source ../../_module4test/method.ds as mod

function print() for Any { }  # in named import, not import user-defined method
