#$test($result = 'type', $lineNum = 4, $chars = 15, $errorKind = 'AttrLimit', $status = 1)

[<CLI CLI() CLI() CLI CLI
  CLI CLI CLI CLI() CLI>]
typedef AAA() {
  var a = ""

    [<Flag>]
    var b = $false

}
