#$test($result = 'type', $errorKind = 'UnfoundReturn', $lineNum = 3, $chars = 10, $status = 1)

function f($a : Int) : String {
    if($a == 3) {
        return ""
    } elif($a > 3) {
        return "hello"
    }
}