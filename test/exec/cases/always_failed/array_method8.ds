#$test($result = 'type', $lineNum = 7, $chars = 12, $errorKind = 'UndefinedMethod', $status = 1)

var s1 = [$SIG_DFL, $SIG_IGN]
var s2 = [$SIG_DFL, $SIG_IGN]
assert $s1 == $s2
assert $s1.equals($s2)
assert $s1.compare($s3) == 0  # undefined