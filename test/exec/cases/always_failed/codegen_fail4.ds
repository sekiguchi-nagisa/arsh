
#$test($result = 'codegen', $lineNum = 6, $chars = 1, $errorKind = 'TooLargeToplevel', $status = 1, $fileName = '../always_failed/codegen_fail4.ds')

echo hello

source $SCRIPT_DIR/codegen_fail1.ds

echo world