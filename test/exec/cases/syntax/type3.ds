##$test($result = 'parse', $lineNum = 3, $errorKind = 'TokenMismatched', $status = 1)


## assert 23 as String != 23 as String # '!=' is recognized as '!' '=' due to type lexing
# now correctly parse '!=' operator

assert 34 is Int != $false
assert 34 as String !~ $/de/