
function f($a : Int) : Int! {
    var l = ['1', '4', '32', '8']
    return for(var i = 0; $i < $l.size(); $i++) {
        $a < 0 && continue
        $a as String == $l[$i] && break $i
    }
}

assert not $f(23)

assert $f(32)! == 2