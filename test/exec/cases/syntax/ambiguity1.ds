#$test($result = 'parse', $lineNum = 3, $chars = 23, $errorKind = 'TYPE', $status = 1)

assert 23 as String < 34 as String  # '<' is recognized as TYPE_OPEN (so following '34' is recognized as invalid token)
