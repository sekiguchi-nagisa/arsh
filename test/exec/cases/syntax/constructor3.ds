#$test($result = 'parse', $lineNum = 5, $chars = 4, $errorKind = 'NoViableAlter', $status = 1)

typedef AAA {
   var a : Int
   typedef Size = Int   # in implicit constructor, only allow 'let' or 'var'
}