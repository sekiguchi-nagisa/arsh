# not allow collection type (Array, Map, Tuple type) in redirection target

#$test($result = 'type', $lineNum = 6, $errorKind = 'Unacceptable')


echo hello > ${["fer", "fr"]} 54fherui   # illegal target