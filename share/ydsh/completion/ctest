#!/usr/bin/env ydsh

# git completion for ydsh

source $SCRIPT_DIR/../module/completion

{
    var path = $findBashCompScript("ctest")
    compdef --cmd ctest --comp-path "$path" --comp-target _ctest
}
