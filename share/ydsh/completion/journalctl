#!/usr/bin/env ydsh

# git completion for ydsh

source $SCRIPT_DIR/../module/completion

{
    var path = $findBashCompScript("journalctl")
    compdef --cmd journalctl --comp-path "$path" --comp-target _journalctl
}
