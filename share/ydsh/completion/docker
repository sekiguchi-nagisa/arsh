#!/usr/bin/env ydsh

# git completion for ydsh

source $SCRIPT_DIR/../module/completion

{
    var path = $findBashCompScript("docker")
    compdef --cmd docker --comp-path "$path" --comp-target _docker
    compdef --cmd dockerd --comp-path "$path" --comp-target _docker
}
